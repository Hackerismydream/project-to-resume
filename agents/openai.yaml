interface:
  display_name: "Project to Resume"
  short_description: "Turn project evidence into truthful resume claims"
  default_prompt: "Use $project-to-resume to audit this project and draft an evidence-backed resume section."
policy:
  allow_implicit_invocation: false
