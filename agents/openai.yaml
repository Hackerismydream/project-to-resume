interface:
  display_name: "Project to Resume"
  short_description: "优化校招项目与实习经历，生成岗位匹配、可直接投递的中文简历"
  default_prompt: "使用 $project-to-resume 根据我提供的简历、项目仓库或 JD，直接生成可投递的中文项目经历，只有必要时再提问。"
policy:
  allow_implicit_invocation: true
