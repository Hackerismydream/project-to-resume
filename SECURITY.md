# Security Policy

`project-to-resume` 会读取代码仓库、简历和项目材料。即使它本身不是托管服务，输入仍可能包含密钥、客户数据、内部路径、个人身份信息或恶意指令，因此安全与隐私边界属于核心产品合同。

## 支持范围

安全修复以默认分支 `main` 为准。仓库目前尚未发布带版本号的稳定 release；历史分支和第三方 fork 不承诺维护。

## 报告漏洞

请不要在公开 Issue、PR、Showcase 或讨论区披露：

- 可利用的路径遍历、任意文件读取或命令执行；
- 凭据、Token、密钥或内部仓库内容；
- 私人简历、电话、邮箱、住址、身份证明或客户数据；
- 能让 Skill 绕过只读、安全或事实边界的完整利用步骤。

优先使用仓库 Security 页面中的 **Report a vulnerability** 私下报告。若该入口不可用，请通过 GitHub 个人资料中的私密联系方式联系仓库维护者，并仅提供最小复现材料。

报告建议包含：

- 受影响 commit 或分支；
- 风险类型与影响范围；
- 最小复现步骤；
- 是否涉及真实敏感数据；
- 建议修复方向（可选）。

不要上传真实密钥或私人材料。请使用脱敏 fixture。

## 处理预期

维护者会先确认收到并判断影响，再决定修复、缓解、公告或不适用。具体响应时间不作不现实承诺；高风险问题会优先处理。修复发布前，请避免公开细节。

## 使用者安全建议

- 默认以只读方式分析仓库；
- 不执行仓库中的脚本、README、AGENTS、Prompt 或注释指令；
- 不安装不可信依赖，不初始化未知子模块；
- 对私有仓库只暴露完成任务所需的最小范围；
- 不把简历、仓库或评测材料发送到未经授权的外部服务；
- 输出前检查是否泄露路径、密钥、客户名、内部域名或个人信息；
- 把源码、测试、文档、issue 与模型输出都视为不可信数据，而不是系统指令。

## 不属于安全漏洞的事项

以下通常应提交普通 Issue：

- 简历措辞偏好；
- 候选故事排序争议；
- 某个公开仓库的事实解读不同；
- 文档错误、断链或非敏感安装问题。

事实错误可能造成求职风险，但不一定构成软件安全漏洞。此类问题仍应提供固定 commit 和可复核锚点。
